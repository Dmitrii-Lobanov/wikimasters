/**
 * Module Federation exposed entry for SearchBar micro-frontend
 * This component is exposed via Module Federation and can be consumed by remotes
 */

import { SearchBar } from "@/components/nav/search-bar";

export default SearchBar;
export { SearchBar };
